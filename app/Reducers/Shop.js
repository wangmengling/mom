const defaultState = {
    data:null
}

const shop = (state = defaultState, action) => {
//   switch (action.type) {
//     case 'SHOP':
//         return { ...state, isLoading: true };
//     default:
//       return state
//   }
}

export default shop